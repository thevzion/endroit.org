{
  "name": "endroit.org",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "description": "The public product site for Endroit.",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro check && astro build",
    "test": "npm run build && node --test tests/*.test.mjs",
    "sync:sources": "node scripts/sync-owned-sources.mjs",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^7.1.3"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "typescript": "^6.0.3"
  }
}
