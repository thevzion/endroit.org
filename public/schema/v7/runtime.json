{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://endroit.org/schema/v7/runtime.json",
  "type": "object",
  "additionalProperties": false,
  "required": ["protocol", "argv", "homeRoot", "equipmentRoot", "resolvedHome", "kernel", "runtimeTrust", "invocation"],
  "properties": {
    "protocol": { "const": "endroit.org/runtime/v2alpha1" },
    "argv": { "type": "array", "items": { "type": "string" } },
    "homeRoot": { "type": "string", "minLength": 1 },
    "deskRoot": { "type": ["string", "null"] },
    "equipmentRoot": { "type": "string", "minLength": 1 },
    "resolvedHome": { "type": "object" },
    "inspection": { "type": "object" },
    "kernel": {
      "type": "object",
      "additionalProperties": false,
      "required": ["runtime", "source", "invoke"],
      "properties": {
        "runtime": { "type": "string" },
        "source": { "enum": ["development", "npm"] },
        "invoke": { "const": "node ./endroit.mjs" }
      }
    },
    "runtimeTrust": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["owner", "namespace", "trust"],
        "properties": {
          "owner": { "type": "string", "minLength": 1 },
          "namespace": { "type": "string", "minLength": 1 },
          "trust": { "enum": ["bundled", "approved", "pending"] },
          "digest": { "type": "string", "pattern": "^sha256:[a-f0-9]{64}$" },
          "error": { "type": "string", "minLength": 1 }
        }
      }
    },
    "invocation": {
      "type": "object",
      "additionalProperties": false,
      "required": ["kind"],
      "properties": {
        "kind": { "enum": ["command", "wake-up"] },
        "provider": { "enum": ["codex", "claude"] }
      }
    }
  }
}
